{
  "_comment": "Copia este archivo a cypress.env.json (ignorado por git) para fijar un usuario de prueba conocido, útil en CI. Si no lo defines, las pruebas registran usuarios únicos vía cy.registerByApi() en cada corrida.",
  "apiUrl": "http://localhost:4000/api",
  "TEST_USER_EMAIL": "cypress.fixed.user@example.com",
  "TEST_USER_PASSWORD": "Test1234!"
}
